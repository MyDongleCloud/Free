passdb passwd-file {
	driver = passwd-file
	default_password_scheme = CRYPT
	auth_username_format = %u
	passwd_file_path = /disk/admin/.modules/mail/password-%d
}

userdb static {
	driver = static
	fields {
		uid = admin
		gid = admin
		home = /disk/admin/.modules/mail/%d/%n
	}
}
